# Sesame-M 使用说明与配置详解

> 面向使用者。本文所有描述都对应仓库当前实现，关键处标注了源码位置，便于自查。
> 安装与免责声明见 [README.md](../README.md)。

---

## 一、安装与启用

1. **前提**：设备已 root，并装好 LSPosed（或兼容框架）。
2. 安装模块 APK。
3. 在 LSPosed 里启用本模块，**作用域勾选「支付宝」（`com.eg.android.AlipayGphone`）**。
4. **重启支付宝**（不是只重启模块），框架才会把新代码注入进去。
5. 打开方式：
   - 桌面图标
   - 或从 LSPosed 的模块列表点进「模块设置」（清单里声明了 `de.robv.android.xposed.category.MODULE_SETTINGS`）

### 需要授予的权限

模块的配置、日志都写在**应用专属的外部目录**里（原因见 §3），因此**不再需要"所有文件访问"**：

| 权限 | 用途 | 怎么给 |
|---|---|---|
| **（已移除）所有文件访问** | 本仓库版本已删除 `MANAGE_EXTERNAL_STORAGE`；配置/日志写在 `Android/media/<宿主包名>/sesame-M/`，导出写在同目录 `export/`，Android 10+ 无需任何存储权限 | 不需要给；Android 9 及以下才会申请读写外部存储 |
| **闹钟权限**（Android 12+） | 定时执行/唤醒用的精确闹钟 | 模块内会提示（`PermissionUtil.checkAlarmPermissions`） |
| **忽略电池优化** | 防止系统冻结导致定时任务漂移 | 受基础电池优化项控制（`AppConfig.batteryPerm`，默认开） |

---

## 二、界面结构与"改动怎么保存"

模块 UI 分四级：

| 级别 | 页面 | 作用 |
|---|---|---|
| 一级 | `MiuixMainActivity` | 首页：运行状态、**日志类目与开关**、各分组入口、扩展页入口 |
| 二级 | `MiuixSettingsActivity` | 某账号下的**模型列表**（按分组归类） |
| 三级 | `MiuixGroupFieldsActivity` | 某个模型的**字段列表** |
| 四级 | `MiuixSelectionEditActivity` | 集合类字段（选择/多选列表）的编辑，支持多项添加 |

### 保存策略（重要）

**改字段只是写内存，不会立刻落盘。** 落盘统一在**离开页面时**完成：

- 三级/二级页退出走 `saveAndFinish()` → `ConfigV2.save(userId, true)`
- **顶部返回键与系统返回键都会走它**，所以正常返回不会丢改动
- 四级页是 dirty 模式：只有确实改过才在返回时 `applyAndSave()`

⚠️ 因此：**改完配置请用返回键退出页面**，不要在改完的瞬间强杀进程。

### 配置按账号隔离

| 文件 | 内容 |
|---|---|
| `config/<uid>/config_v2.json` | 该支付宝账号的模型配置 |
| `config/<uid>/status.json` | 该账号的当日状态标记（每日自动重置） |
| `config_v2.json`（根目录） | 未识别账号时的默认配置 |
| `statistics.json` | 统计（**全局一份，不按账号分**） |

`<uid>` 是支付宝账号 ID。模块会给每个账号分配一个只增不改的序号，日志里以 `[账号1]`、`[账号2]` 形式出现（对应 `accountIndex.json`）。

> **配置字段增删是安全的**：`ConfigV2.setModelFieldsMap()` 只遍历当前模型声明的字段并搬运旧值，被删除的旧 key 会自动丢弃、不会报错；新增字段首次加载为空。

---

## 三、数据目录一览

主目录：`/sdcard/Android/media/com.eg.android.AlipayGphone/sesame-M/`

> 用**支付宝的包名**做目录，是为了让「注入进支付宝的模块代码」和「模块自己的 App」两个进程共享同一份数据 —— 它们 UID 不同，只能通过公共位置交换数据（`FileUtil.getMainDirectoryFile()`，`AppConfig` 里也有对应注释）。

```
sesame-M/
├── appConfig.json          界面与日志开关
├── config_v2.json          默认配置（未分账号时用）
├── statistics.json         统计（全局）
├── token_config.json       答题题库 / 光盘图片 / 自定义路线队列
├── config/
│   └── <uid>/
│       ├── config_v2.json  该账号配置
│       └── status.json     该账号当日状态
├── log/                    日志（见 §5）
├── bak/                    配置滚动备份（见 §9）
└── *.json                  各类 ID 映射缓存（好友、任务列表、城市码等）
```

另有导出目录：`/sdcard/Android/media/com.eg.android.AlipayGphone/sesame-M/export/`
（统计/日志的导出出口；本仓库版本不再写公共 `Download`，因此无需"所有文件访问"权限）。
调试服务开启后，令牌与实际端口写在同目录的 `debug_server.txt`（见 §5.5）。

---

## 四、配置框架说明

### 4.1 分组

`ModelGroup`：**基础** / 森林 / 庄园 / 新村 / 农场 / 金豆 / 运动 / 会员 / 其他。

### 4.2 模型（按注册顺序）

`ModelOrder` 决定顺序：

```
BaseModel(基础) → AntForestV2(森林) → AntFarm(庄园) → AntStall(新村)
→ AntOrchard(农场肥料) → ProtectEcology(森林-保护生态) → AntDodo(神奇物种)
→ AntOcean(神奇海洋) → AntSports(运动) → AntMember(会员) → GreenFinance(绿色经营)
→ goldenbeans(金豆) → AnswerAI(AI答)
```

每个模型有自己的「启用」开关（在三级页顶部）；关掉后该模块的自动化不执行。

### 4.3 字段类型

| 类型 | 交互 |
|---|---|
| 布尔 | 开关 |
| 整数 / 整数(倍数) | 数字输入（部分带上下限） |
| 字符串 / 文本 | 输入框 |
| 单选（Choice） | 下拉选择，如「捐赠一个项目/所有项目」 |
| 多选（Select） | 弹四级页勾选，支持搜索/长按移除 |
| 单选列表（SelectOne） | 同上但只能选一个 |
| 多选+次数（SelectAndCount / …One） | 每个条目可配一个次数/额度 |
| 列表（List） | 逗号分隔的多值，如时间列表 |
| 按钮（Empty） | 点击即执行动作，如「测试响应」「清空」 |

**依赖联动**：很多字段带 `setDependsOn("某个开关的code")`，例如「小鸡睡觉 | 时间」依赖「小鸡睡觉 | 允许睡觉」。**被依赖的开关关掉时，这些子项不会生效**，配置页里也可能折叠不显示。

---

## 五、日志：从哪看、怎么看

### 5.1 位置与文件名

目录：`/sdcard/Android/media/com.eg.android.AlipayGphone/sesame-M/log/`

每个类目一个文件，**按日期分文件**：`<类目>.<yyyy-MM-dd>.log`

| 类目 | 文件名 | 默认开关 | 内容 |
|---|---|---|---|
| 森林记录 | `forest.<date>.log` | **开** | 森林能量收取、帮收、浇水等 |
| 庄园记录 | `farm.<date>.log` | **开** | 庄园喂养、捐蛋、家庭、答题等 |
| 金豆记录 | `goldenbeans.<date>.log` | **开** | 金豆夺宝相关 |
| 其他记录 | `other.<date>.log` | **开** | 其它模块的动作与**诊断提示** |
| 抓包记录 | `debug.<date>.log` | 关 | 抓包（`Log.debug`），默认不写 |
| 异常日志 | `error.<date>.log` | **开** | 异常堆栈 |
| 运行日志 | `runtime.<date>.log` | **开** | 主日志：模块调度、配置读写、状态等 |

开关在**一级页面**（`MiuixMainActivity` 的「日志」区）。对应的存储字段见 `AppConfig`（`enableForestLog` / `enableFarmLog` / `enableGoldenBeansLog` / `enableOtherLog` / `enableDebugLog` / `enableViewErrorLog` / `enableViewRuntimeLog`）。

### 5.2 写入规则（哪种日志进哪个文件）

| 代码方法 | 运行日志 | 分类文件 | 受哪些开关控制 |
|---|---|---|---|
| `Log.i` / `Log.record` / `Log.system` | ✅ | — | 查看运行日志 |
| `Log.forest` / `Log.farm` / `Log.goldenBeans` / `Log.other` | ✅ | ✅ | 运行日志 + 对应分类开关（**两边都关则不写**） |
| `Log.debug` | — | ✅（debug 文件） | 抓包记录（默认关） |
| `Log.error` / `Log.err` | ✅ | ✅（error 文件） | 查看异常日志 + 查看运行日志 |

> 所以"我在日志里找不到"通常有两种原因：① 该分类开关关了；② 该分类方法只写分类文件、而你看的是运行日志。
> **诊断提示类信息走 `Log.other`（默认开）**，排查时优先看「其他记录」。

### 5.3 行格式

```
13:34:10.251 RUNTIME: [账号1]执行结束-庄园
19:37:33.150 [账号1]森林签到...
```

- 前缀是 `时:分:秒.毫秒`，随后是 tag（`RUNTIME` / `FOREST` / `FARM` / …），再是正文
- 正文前的 `[账号N]` 是账号简称，便于多账号区分
- 日志**不写 uid、不写本账号昵称**（好友用昵称），便于对外分享

### 5.4 在 App 里查看

一级页点某个类目 → 进入日志查看页：

- 支持**搜索**
- **运行日志页额外支持按 tag 过滤**（顶部 Chip，显示各 tag 出现次数）
- 支持**分享**（右上角走系统分享，用 FileProvider 授权，不会暴露其它文件）

### 5.5 日志清理规则

- **关闭某个类目开关时，会顺带清空该类目的历史日志** —— 这是刻意设计，不是 bug
- 每轮任务执行结束后会调用一次清理：**删除所有非当天日志**；当天日志只有在体积 **≥100 MiB** 时才删（`FileUtil.clearLog()`）

### 5.6 用电脑取日志

```bash
adb pull /sdcard/Android/media/com.eg.android.AlipayGphone/sesame-M/log ./log
```

> Windows 下看中文日志若乱码，用支持 UTF-8 的编辑器打开（PowerShell 5.1 默认 GBK 会乱码）。

---

## 六、自动黑名单

### 6.1 它是干什么的

某些任务在**当前账号/当前版本**下注定做不了（服务端明确不支持、活动当天没配置等）。如果每轮都去试，会产生大量无效请求和误导性的失败日志。**自动黑名单**就是把这类条目自动停掉，并定期放出来复核。

入口是 `MessageUtil.checkResultCodeAndMarkTaskBlackList(列表名, 任务名, 响应)`。

### 6.2 判定规则

**先排除可重试错误**（这类只是临时故障，一律不拉黑）：

- 响应里 `retryable` / `retriable` 为 true
- 或 `code`/`resultCode`/`errorCode` 为 `3000`、`REMOTE_INVOKE_EXCEPTION`
- 或文案含「服务器正在开小差」，或 `resultCode=102`（服务端繁忙）

**再按文案判定**，关键字是「不支持rpc调用」：

| 命中位置 | 处理 |
|---|---|
| `desc` 字段 | **立即拉黑**（判据强） |
| 其它字段（`resultDesc` / `resultView` / `memo` / `errorMsg` / `errorMessage` / `resultMsg`） | **需连续确认**：同一项在 3 天窗口内命中满 3 次才真正拉黑；未达标只记录一条「可疑错误第 n/3 次命中」 |

### 6.3 生命周期（四态）

```
尝试 → 失败 → 拉黑 ──(满 3 天)──> 自动解禁、重试一次 ──(再失败，累计满 3 次)──> 永久拉黑
                    └─ 期间每次再失败都重新计时
```

| 参数 | 值 |
|---|---|
| 连续确认次数 | 3 次 |
| 确认窗口 | 3 天（超窗口未再命中则重新计数） |
| 拉黑后解禁 | 3 天后自动解禁并重试 |
| 解禁重试上限 | 3 次，超过则**永久拉黑**，不再自动解禁 |

**只处理模块自动加入的条目**：你自己手动加进黑名单的项，不会被自动解禁打扰。

### 6.4 存储位置

`AutoBlackListMap` 文件，键与值：

```
键：<模块>|<列表名>|<任务名>
值：命中次数;最后命中天;拉黑天;重试次数      拉黑天 = -1 表示永久拉黑
```

### 6.5 受管的 13 个列表

森林活力值任务、森林抽抽乐任务、庄园饲料任务、庄园装扮抽抽乐任务、神奇物种任务、神奇海洋普通任务、神奇海洋去摸鱼任务、农场肥料任务、金豆夺宝任务、新村任务、运动任务、会员任务、会员芝麻信用任务。

其中 4 个列表有额外的判据（错误码/特有文案），例如：

- **运动任务**：`TASK_ID_INVALID`、「海豚活动触发不可重试错误」
- **农场肥料任务**：「任务全局配置不存在」
- **会员芝麻信用任务**：「不是有效的入参」「存在进行中的生活记录」「生活记录模板不存在」
- **金豆夺宝任务**：`400000040`、`20020012`、`TASK_ID_INVALID`、`ILLEGAL_ARGUMENT`

### 6.6 在日志里怎么认

关键字：`自动拉黑🏴`（已拉黑）、`自动拉黑🕵️`（疑似、待确认）、`自动解禁🕊️`（解禁重试）、`自动拉黑🔒`（永久拉黑）、`黑名单治理🧹`（原预置项改为自动判定）。

### 6.7 想让它重新尝试

到对应模块的对应列表里，把该项**手动移除**即可；之后走正常流程（若再失败会重新拉黑并重新计时）。

---

## 七、AI 答（答题）

**「其他」组 → AI答**，配置项：

| 配置项 | 说明 |
|---|---|
| 自定义AI \| 接口地址(根地址,如/v1) | 填根地址即可，会自动补 `/chat/completions`（已带该路径则原样使用） |
| 自定义AI \| 模型名 | 如 `deepseek-chat` |
| 自定义AI \| 令牌 | Bearer 令牌 |
| 自定义AI \| 输出Token上限(0=不发) | 默认 1024；**填 0 表示不发送该参数**（部分新模型只认 `max_completion_tokens`，填 0 可绕过） |
| 自定义AI \| 测试响应 | **点一下就能验证地址/模型/令牌是否可用**，结果用气泡回显 |

### 生效范围

| 场景 | 是否走 AI |
|---|---|
| 庄园答题 | ✅ |
| 蚂蚁新村 · 每日答题 | ✅ |
| 神奇海洋 · 答题学海洋知识 | ✅ |
| 会员 · 每日问答 | ❌ 服务端直接返回正确答案 |
| 会员 · 视频问答 | ❌ 固定答案 |

### 题库（自动纠错）

模块会把**服务端回传的次日题目与正确答案**存到 `token_config.json`，答题时命中即用题库答案覆盖 AI 的结果（**前提是该答案仍在候选选项内**，题目变了会忽略纠错）。

- 题库**按天滚动**：只保留今天与明天两天的条目，不会无限增长
- **AI 的回答不落盘**，只提交给服务端

### 未配置 AI 时

不会报错：直接使用题库答案；再没有就用第一个选项，并在日志里记录原因（`兜底回答🤖`）。

---

## 八、扩展页（其它实用功能）

「其他」组里的扩展入口（`MiuixExtensionsActivity`）：

- **清空光盘行动图片**：清掉已缓存的餐前/餐后图片组合
- **自定义行走路线**：维护路线列表与待行走队列（队列是用一条少一条）

---

## 九、配置备份与恢复

- 目录：`sesame-M/bak/`
- 文件：`config_v2_<uid>_<A|B|C…>.json`
- 策略：**每天最多备份一次**，按 A→B→C… 顺序**滚动覆盖**（不是追加）
- 份数：由「基础 | 按天和修改备份配置保存数(滚动覆盖)」控制，默认 **5**，上限 26

恢复：把想要的备份文件内容覆盖回 `config/<uid>/config_v2.json`（或根目录 `config_v2.json`）后重启支付宝。

---

## 十、配置项说明

**全部 13 个模型、296 个配置项的逐项清单见 → [配置项说明.md](配置项说明.md)**（由脚本从源码自动生成，含 code、显示名称、默认值/范围、依赖关系）。

下面把最常用的「基础」模型列出来（`BaseModel`）：

| 配置项 | 默认 | 说明 |
|---|---|---|
| 保持唤醒 | 开 | 保持 CPU 唤醒，保证定时任务准时 |
| 执行间隔(分钟) | 50（1~720） | 常规轮次间隔 |
| 定时执行 | `065530,2359,24` | 在这些时刻各执行一次。时间格式为数字串：`HHmmss`（`065530`=06:55:30）、`HHmm`（`2359`=23:59）、`HH`（`24`=24 点）；填 `-1` 关闭本项 |
| 定时唤醒 | `0650,2350` | 在这些时刻唤醒（不执行任务）；`-1` 关闭 |
| 只收能量时间(范围) | `0700-0731` | 限定只做收能量的时间段 |
| 定时任务模式 | 系统计时 | 系统计时 / 程序计时 |
| 超时重启 | 开 | 任务超时后重启任务 |
| 异常等待时间(分钟) | 60（0~1440） | 异常后等待多久再继续 |
| 按天和修改备份配置保存数(滚动覆盖) | 5 | 备份份数，上限 26 |
| 使用新接口 | 开 | 需支付宝 ≥ v10.3.96.8100 |
| 开启抓包 | 关 | 开启后写「抓包记录」日志 |
| RPC请求列表及每日执行数 | 空 | ⚠️ 慎用：按配置自动调用 RPC |
| RPC可选任务列表 | 空 | 配合上项；内容需打开 `rpcResquest.json` 配置 |
| 气泡提示 | 开 | 执行结果用气泡提示 |
| 气泡纵向偏移 | 0 | 气泡位置微调 |
| 开启状态栏禁删 | 关 | 保护通知不被划掉 |

---

## 十一、调试服务（本仓库新增开关，默认全关）

模块内有一个仅供本机使用的调试 HTTP 服务，**默认不启动**，需要在一级页「日志 → 调试服务」里逐项打开：

| 开关 | 默认 | 说明 |
|---|---|---|
| 本地调试 HTTP 服务 | 关 | 总开关；打开后服务只监听 `127.0.0.1`，端口默认随机取 20000~40000 |
| 开放 `/debugHandler` | 关 | ⚠️ 风险最高：请求方可以指定任意宿主 RPC 方法名，等于借你的支付宝身份发请求 |
| 开放授权码/标记路由 | 关 | `/getAuthCode` 与 `/getAlipayMiniMark`（当前宿主版本下 `getAuthCode` 恒失败） |

- 令牌由模块用 `SecureRandom` 随机生成（不再硬编码在源码里），可在页面「调试令牌」一行看到，
  同时写在 `sesame-M/debug_server.txt`（含令牌与实际端口）。
- 请求需带 `Authorization: Bearer <令牌>`；从电脑访问先 `adb forward tcp:<端口> tcp:<端口>`。
- 关闭开关并重启支付宝后服务即停止。

---

## 十二、常见问题排查

**Q：改了配置没生效？**
A：用返回键退出页面让配置落盘，然后**重启支付宝**（跨进程配置靠广播通知重载，重启最稳）。

**Q：日志是空的 / 找不到我关心的内容？**
A：① 确认一级页对应类目的开关是开的；② 分类方法只写分类文件，别只看运行日志；③ 诊断类信息在「其他记录」；④ 关掉某类开关会**清空该类历史日志**。

**Q：某个任务一直失败但日志看不出原因？**
A：看「其他记录」与「异常日志」；服务端繁忙（102）会被日志降噪（同 tag 前 3 次打印，之后静默），不是没发生。

**Q：某任务突然不做了，也不报错？**
A：很可能被自动黑名单停掉了。在「其他记录」里搜 `自动拉黑`，并到对应列表里把该项手动移除即可恢复。

**Q：多个支付宝账号会互相覆盖配置吗？**
A：不会，配置与状态按 uid 分目录（`config/<uid>/`）。但 `statistics.json`（统计）是全局一份。

---

## 附：本文档的事实来源

- 主要依据仓库源码（`app/src/main/java/io/github/aw1y2z/sesame/`），关键结论均标注了实现位置，如 `FileUtil`（目录与备份）、`Log`（写入规则）、`AppConfig`（日志开关）、`MessageUtil`（自动黑名单）、`BaseModel`（基础项）、`ModelOrder` / `ModelGroup`（模型与分组）。
- **未实机验证**的说明：AI 答的接口兼容性（不同服务商差异）、自动黑名单中各列表的特殊判据（依赖服务端实际文案）。
- 如果文档与实现不一致，**以源码为准**，并欢迎指出以便修正。
